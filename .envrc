# Direnv configuration for Time-Lab
# Automatically loads the Nix development environment when you cd into this directory
#
# To enable: direnv allow

# Use the default Nix development shell
use flake

# Optional: Load local environment variables from secrets/.env.lab
if [ -f secrets/.env.lab ]; then
  dotenv secrets/.env.lab
fi

# Print info when environment loads
echo "✅ Time-Lab environment loaded"
echo "   Run 'just --list' to see available commands"

